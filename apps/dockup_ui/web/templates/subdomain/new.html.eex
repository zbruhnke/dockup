<h2>New Subdomain</h2>

<%= render "form.html", changeset: @changeset,
                        action: subdomain_path(@conn, :create) %>

<span><%= link "Back", to: config_path(@conn, :index) %></span>
