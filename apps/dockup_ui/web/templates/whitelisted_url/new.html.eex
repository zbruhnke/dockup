<div class="c-container-700">
  <h2>New whitelisted url</h2>

  <%= render "form.html", changeset: @changeset,
                          action: whitelisted_url_path(@conn, :create) %>

  <span><%= link "Back", to: config_path(@conn, :index), class: "c-btn c-btn--default c-btn--xs" %></span>
</div>
